# Sentiment Enricher: English Keywords
This module extends the [Sentiment TTRPG System](https://github.com/tomlaflin/sentiment)'s built in Text Enricher capabilities, allowing it to highlight certain Sentiment Keywords.

## Installation
### Foundry's "Install Module" Dialog
Paste this link into the "Install Module" Dialog's "Manifest URL" field and click "Install":<br>
`https://raw.githubusercontent.com/konokonekonoko/sentiment-enricher-english-keywords/main/module.json`

Then enable the module in your world.

### Manual
[Download this repo as a zip](https://github.com/konokonekonoko/sentiment-enricher-english-keywords/archive/refs/heads/main.zip) and unzip it in your `FoundryVTT/Data/modules` directory. Usually `%user%\AppData\Local\FoundryVTT\Data\modules` on Windows or `/home/$USER/.local/share/FoundryVTT/Data/modules` on Linux. Then remove the `-main` from the folder name.

The directory structure should be as follows:
```
FoundryVTT/
└ Data/
  ├ modules/
  │ ├ sentiment-enricher-english-keywords/
  │ └ [other module directories]/
  └ [other directories]/
```
Then enable the module in your world.
